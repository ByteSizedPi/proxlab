# Shared across every stack in this repo — values here are identical for all apps.
# Local testing: docker compose --env-file ../common.env --env-file .env up
# Once deployed via Komodo: these move into Settings > Variables instead, referenced
# per-stack as [[PUID]], [[CONFIG_ROOT]], etc. — this file stops being the source of
# truth in production, it's just here so compose files can be tested standalone first.

# --- Identity ---
PUID=1000
PGID=1000
TZ=Africa/Johannesburg

# --- Storage roots ---
# CONFIG_ROOT/<app> = each app's own settings/database, isolated per app.
# DATA_ROOT = ONE path mounted at /data identically in every *arr + download
# client container, so Radarr/Sonarr can hardlink/atomic-move files from the
# download client's folder into the media library instead of copying across
# a filesystem boundary. Do NOT give each app its own separate data subpath.
CONFIG_ROOT=/mnt/docker-data/appdata
DATA_ROOT=/mnt/docker-data/data
