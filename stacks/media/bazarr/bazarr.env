# Bazarr-specific, NON-SECRET config. This file IS committed.
#
# Shared values (PUID/PGID/TZ/CONFIG_ROOT/DATA_ROOT) live in ../../common.env.
#
# Subtitle provider accounts (OpenSubtitles.com API token, etc.) are
# credentials — set them by hand in Bazarr's own UI after it's up, not here.
# See arr-stack-buildout memory for jjserver's specific failure mode
# (opensubtitles.org vs .com are separate accounts).

# Nothing Bazarr-specific needed yet.
