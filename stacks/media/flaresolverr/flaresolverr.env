# FlareSolverr-specific, NON-SECRET config. This file IS committed.
#
# Shared values (PUID/PGID/TZ/CONFIG_ROOT/DATA_ROOT) live in ../../common.env.
# FlareSolverr's own image is not linuxserver-based, so it has no PUID/PGID.

# Nothing FlareSolverr-specific needed yet.
