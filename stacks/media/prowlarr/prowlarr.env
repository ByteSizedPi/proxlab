# Prowlarr-specific, NON-SECRET config. This file IS committed.
#
# Shared values (PUID/PGID/TZ/CONFIG_ROOT/DATA_ROOT) live in ../common.env.
# Values here win over common.env — both are passed as --env-file, later wins.
#
# Secrets (API keys, passwords) do NOT go here. Add them as Komodo Variables
# marked "secret", then reference them in compose.yaml as [[PROWLARR_API_KEY]].

# Nothing Prowlarr-specific needed yet.
