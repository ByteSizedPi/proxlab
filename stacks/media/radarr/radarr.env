# Radarr-specific, NON-SECRET config. This file IS committed.
#
# Shared values (PUID/PGID/TZ/CONFIG_ROOT/DATA_ROOT) live in ../../common.env.
#
# Secrets (API key, once generated on first start) do NOT go here — see
# README.md for the Komodo Variables pattern if this ever needs one.

# Nothing Radarr-specific needed yet.
