# Netdata-specific, NON-SECRET config. This file IS committed.
#
# Shared values (TZ/CONFIG_ROOT/DATA_ROOT/ADMIN_DOMAIN) live in
# ../../common.env.
#
# Not claimed to Netdata Cloud on purpose — that needs a claim token typed
# in through the Cloud UI and adds an external dependency this stack does
# not need for local dashboards/alerts. Claiming later is additive: set
# NETDATA_CLAIM_URL / NETDATA_CLAIM_TOKEN / NETDATA_CLAIM_ROOMS as Komodo
# secret Variables and reference them here the same way traefik.env does.

# Nothing else Netdata-specific needed yet.
